# Пароли в git — только для демо! В реальной жизни: SealedSecrets,
# External Secrets Operator, Vault и т.п.
apiVersion: v1
kind: Secret
metadata:
  name: postgres
  labels:
    app: postgres
type: Opaque
stringData:
  POSTGRES_USER: demo
  POSTGRES_PASSWORD: demo-password
  POSTGRES_DB: demo
